import { test, expect } from "bun:test";

// Placeholder to make "bun test tests/" exit 0 for TEST_RUNNER && chain.
// Real contract tests added in ListeningIndicator / MicUtteranceCapture / VoiceErrorSurface phases.

test("voice smoke - bun test runner active", () => {
  expect(true).toBe(true);
});
